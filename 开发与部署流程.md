# 商城后台多商户系统 - 开发与部署流程

## 一、项目结构

```
goshopadmin/
├── backend/                # Go后端服务（后台管理系统）
│   ├── cache/              # 缓存工具（布隆过滤器、分布式锁、多级缓存）
│   ├── config/             # 配置管理
│   ├── constants/          # 常量定义
│   ├── controllers/        # 控制器
│   ├── errors/             # 错误码定义
│   ├── middleware/         # 中间件
│   ├── models/             # 数据模型
│   ├── routes/             # 路由定义
│   ├── services/           # 业务服务层
│   ├── utils/              # 工具类
│   ├── .env.example        # 环境变量示例
│   ├── go.mod              # Go模块依赖
│   └── main.go             # 应用入口
├── shop-backend/           # Go后端服务（C端商城）
│   ├── cache/              # 缓存工具（布隆过滤器、熔断器、分布式锁、多级缓存）
│   ├── config/             # 配置管理
│   ├── constants/          # 常量定义
│   ├── controllers/        # 控制器
│   ├── errors/             # 错误码定义
│   ├── middleware/         # 中间件
│   ├── models/             # 数据模型
│   ├── pkg/mq/             # RabbitMQ封装（连接池、消费者、生产者、延迟队列）
│   ├── routes/             # 路由定义
│   ├── services/           # 业务服务层
│   ├── utils/              # 工具类
│   ├── .env.example        # 环境变量示例
│   ├── go.mod              # Go模块依赖
│   └── main.go             # 应用入口
├── search-service/         # 搜索微服务
│   ├── .env.example        # 环境变量示例
│   ├── go.mod              # Go模块依赖
│   └── main.go             # 应用入口
├── docker/                 # Docker配置
│   ├── mysql/              # MySQL配置
│   ├── nginx/              # Nginx配置
│   ├── elk/                # ELK日志系统配置
│   │   ├── elasticsearch/  # Elasticsearch配置
│   │   ├── filebeat/       # Filebeat配置
│   │   ├── kibana/         # Kibana配置
│   │   └── logstash/       # Logstash配置
│   ├── grafana/            # Grafana仪表盘配置
│   ├── prometheus/         # Prometheus监控配置
│   └── docker-compose.yml  # 容器编排配置
├── frontend/               # Vue前端应用（管理端）
│   ├── public/             # 静态资源
│   ├── src/                # 源代码
│   │   ├── api/            # API接口
│   │   ├── assets/         # 静态资源
│   │   ├── components/     # 可复用组件
│   │   ├── directives/     # 自定义指令
│   │   ├── router/         # 路由配置
│   │   ├── store/          # 状态管理
│   │   ├── views/          # 页面级组件
│   │   ├── App.vue         # 根组件
│   │   └── main.js         # 应用入口
│   ├── index.html          # HTML入口
│   ├── package.json        # 前端依赖
│   └── vite.config.js      # Vite配置
├── shop-frontend/          # C端商城前端应用
│   ├── public/             # 静态资源
│   ├── src/                # 源代码
│   │   ├── api/            # API接口
│   │   ├── assets/         # 静态资源
│   │   ├── components/     # 可复用组件
│   │   ├── router/         # 路由配置
│   │   ├── store/          # 状态管理
│   │   ├── views/          # 页面级组件
│   │   ├── App.vue         # 根组件
│   │   └── main.js         # 应用入口
│   ├── index.html          # HTML入口
│   ├── package.json        # 前端依赖
│   └── vite.config.js      # Vite配置
├── docs/                   # 项目文档
└── 项目架构与开发部署文档.md  # 架构设计文档
```

## 二、环境运行逻辑

### 2.1 开发环境

| 服务 | 容器名 | 技术栈 | 代码同步 | 热更新 | 访问地址 |
|------|--------|--------|----------|--------|----------|
| 后端 | goshopadmin-backend | golang:1.24-alpine | backend → /app | 需重启容器 | http://localhost:8080 |
| C端后端 | goshopadmin-shop-backend | golang:1.24-alpine | shop-backend → /app | 需重启容器 | http://localhost:8081 |
| 搜索服务 | goshopadmin-search-service | golang:1.24-alpine | search-service → /app | 需重启容器 | http://localhost:8082 |
| 前端(管理端) | goshopadmin-frontend | node:20-alpine | frontend → /app | Vite自动热更新 | http://localhost:5173 |
| C端前端 | goshopadmin-shop-frontend | node:20-alpine | shop-frontend → /app | Vite自动热更新 | http://localhost:3001 |
| MySQL | goshopadmin-mysql | mysql:8.0 | 持久化到 docker/data/mysql | - | 3306 |
| Redis | goshopadmin-redis | redis/redis-stack-server:7.2.0-v13 | 持久化到 docker/data/redis | - | 6379 |
| RabbitMQ | goshopadmin-rabbitmq | rabbitmq:3.10-management | 持久化到 docker/data/rabbitmq | - | 5672/15672 |
| Nginx | goshopadmin-nginx | nginx:1.20 | - | - | 80/443 |
| Prometheus | goshopadmin-prometheus | prom/prometheus:latest | - | - | 9090 |
| Grafana | goshopadmin-grafana | grafana/grafana:latest | - | - | 3000 |
| Elasticsearch | goshopadmin-elasticsearch | custom:8.17.0 | - | - | 9200 |
| Logstash | goshopadmin-logstash | logstash:8.17.0 | - | - | - |
| Kibana | goshopadmin-kibana | kibana:8.17.0 | - | - | 5601 |
| Filebeat | goshopadmin-filebeat | elastic/filebeat:8.17.0 | - | - | - |

### 2.2 生产环境

- **后端**：多阶段构建，使用预构建的可执行文件运行
- **前端/C端**：构建产物存储在 `dist` 目录，Nginx提供静态文件服务
- **Nginx**：作为反向代理，处理静态文件和API请求转发
- **ELK**：集中式日志收集与分析
- **Prometheus + Grafana**：服务监控与可视化

## 三、API配置

### 3.1 前端API配置（管理端）

**文件**：`frontend/src/api/auth.js`

```javascript
const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || '/api';
```

**Vite配置**（`frontend/vite.config.js`）：

```javascript
server: {
  port: 3000,
  proxy: {
    '/api': {
      target: env.VITE_API_PROXY_TARGET || 'http://backend:8080',
      changeOrigin: true
    }
  }
}
```

**环境变量**（`frontend/.env.example`）：

```
VITE_API_BASE_URL=/api
VITE_API_PROXY_TARGET=http://backend:8080
```

### 3.2 C端API配置

**文件**：`shop-frontend/src/api/index.js`

```javascript
const API_BASE_URL = '/api';
```

**Vite配置**（`shop-frontend/vite.config.js`）：

```javascript
server: {
  port: 3001,
  proxy: {
    '/api': {
      target: env.VITE_API_PROXY_TARGET || 'http://localhost:8081',
      changeOrigin: true
    }
  }
}
```

**环境变量**（`shop-frontend/.env.example`）：

```
VITE_API_BASE_URL=/api
VITE_API_PROXY_TARGET=http://shop-backend:8081
VITE_WS_PROXY_TARGET=ws://shop-backend:8081
```

### 3.3 Nginx配置

**文件**：`docker/nginx/nginx.conf`

```nginx
upstream backend {
    server goshopadmin-backend:8080;
}

upstream shop-backend {
    server goshopadmin-shop-backend:8081;
}

# 管理端
server {
    listen 80;
    server_name admin.localhost;
    root /usr/share/nginx/html/admin;
    index index.html;

    client_max_body_size 10M;

    # Gzip 压缩
    gzip on;
    gzip_min_length 1024;
    gzip_comp_level 6;

    # 安全头
    add_header X-Content-Type-Options nosniff;
    add_header X-Frame-Options DENY;
    add_header X-XSS-Protection "1; mode=block";

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://backend/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /health {
        proxy_pass http://backend/health;
    }
}

# C端商城
server {
    listen 80;
    server_name shop.localhost;
    root /usr/share/nginx/html/shop;
    index index.html;

    client_max_body_size 10M;

    # Gzip 压缩
    gzip on;
    gzip_min_length 1024;
    gzip_comp_level 6;

    # 安全头
    add_header X-Content-Type-Options nosniff;
    add_header X-Frame-Options DENY;
    add_header X-XSS-Protection "1; mode=block";

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /ws {
        proxy_pass http://shop-backend/ws;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 86400;
    }

    location /api/ {
        proxy_pass http://shop-backend/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /health {
        proxy_pass http://shop-backend/health;
    }
}
```

## 四、开发流程

### 4.1 环境准备

1. **安装Docker**：确保本地安装了Docker和Docker Compose
2. **克隆代码**：从GitHub克隆项目代码
3. **配置环境变量**：
   - 后端：复制 `.env.example` 为 `.env` 并修改配置
   - C端后端：复制 `.env.example` 为 `.env` 并修改配置
   - 搜索服务：复制 `.env.example` 为 `.env` 并修改配置
   - 前端：无需特殊配置

### 4.2 启动开发环境

```bash
# 进入docker目录
cd docker

# 启动所有服务
docker-compose up -d

# 查看服务状态
docker-compose ps

# 查看日志
docker-compose logs -f
```

### 4.3 开发工作流

#### 后端开发（后台管理系统）
1. 修改 `backend` 目录下的代码
2. 代码会自动同步到容器中
3. 重启容器使代码生效：`docker restart goshopadmin-backend`
4. 通过 `http://localhost:8080` 访问API

#### 后端开发（C端商城）
1. 修改 `shop-backend` 目录下的代码
2. 代码会自动同步到容器中
3. 重启容器使代码生效：`docker restart goshopadmin-shop-backend`
4. 通过 `http://localhost:8081` 访问API

#### 搜索服务开发
1. 修改 `search-service` 目录下的代码
2. 代码会自动同步到容器中
3. 重启容器使代码生效：`docker restart goshopadmin-search-service`
4. 通过 `http://localhost:8082` 访问搜索API

#### 前端开发（管理端）
1. 修改 `frontend` 目录下的代码
2. 代码会自动同步到容器中
3. Vite会自动热更新
4. 通过 `http://localhost:5173` 访问前端

#### C端前端开发
1. 修改 `shop-frontend` 目录下的代码
2. 代码会自动同步到容器中
3. Vite会自动热更新
4. 通过 `http://localhost:3001` 访问C端

### 4.4 代码提交

1. **代码规范**：遵循Go和Vue的代码规范
2. **提交信息**：使用清晰的提交信息
3. **分支管理**：遵循GitFlow分支管理规范

## 五、部署流程

### 5.1 构建生产镜像

```bash
# 进入docker目录
cd docker

# 构建所有服务
docker-compose build
```

### 5.2 部署生产环境

#### 配置修改
- 修改 `docker-compose.yml` 中的环境变量
- 确保数据持久化目录存在
- 配置Nginx反向代理（修改 `server_name` 为实际域名）

#### 启动服务

```bash
# 启动生产服务（设置环境变量）
NODE_ENV=production RESTART_POLICY=always docker-compose up -d

# 查看服务状态
docker-compose ps

# 查看日志
docker-compose logs -f
```

### 5.3 部署验证

1. **后端API**：访问 `http://服务器IP:8080/health` 验证健康状态
2. **C端API**：访问 `http://服务器IP:8081/health` 验证健康状态
3. **搜索服务**：访问 `http://服务器IP:8082/health` 验证健康状态
4. **前端应用（管理端）**：访问 `http://admin.服务器IP` 验证前端是否正常
5. **C端商城**：访问 `http://shop.服务器IP` 验证C端是否正常
6. **数据库连接**：验证MySQL、Redis、RabbitMQ连接正常
7. **监控面板**：访问 `http://服务器IP:3000` 验证Grafana是否正常
8. **日志系统**：访问 `http://服务器IP:5601` 验证Kibana是否正常

### 5.4 监控与维护

1. **日志管理**：使用 `docker-compose logs` 查看服务日志，或通过Kibana查看集中日志
2. **服务监控**：通过 Prometheus + Grafana 监控服务状态
3. **备份策略**：定期备份 `docker/data` 目录
4. **更新部署**：
   - 停止服务：`docker-compose down`
   - 拉取最新代码：`git pull`
   - 重新构建：`docker-compose build`
   - 启动服务：`docker-compose up -d`

## 六、API配置验证与技术优势

### 6.1 验证方法

#### 开发环境
1. 启动所有Docker服务：`docker-compose up -d`
2. 打开浏览器访问 `http://localhost:5173`（管理端）
3. 打开浏览器访问 `http://localhost:3001`（C端）
4. 测试API请求：
   - 打开浏览器开发者工具
   - 检查网络请求，确认管理端API请求通过 `localhost:5173/api` 发送
   - 确认C端API请求通过 `localhost:3001/api` 发送
   - 验证请求是否成功

#### 生产环境
1. 构建前端项目：`npm run build`
2. 启动Docker容器：`docker-compose up -d`
3. 打开浏览器访问 `http://admin.服务器IP`（管理端）
4. 打开浏览器访问 `http://shop.服务器IP`（C端）
5. 测试API请求：
   - 打开浏览器开发者工具
   - 检查网络请求，确认API请求通过对应域名 `/api` 路径发送
   - 验证请求是否成功

### 6.2 技术优势

1. **环境适配**：无论是开发环境还是生产环境，都使用相同的API路径
2. **跨域处理**：开发环境通过Vite代理处理跨域，生产环境通过Nginx处理
3. **配置集中**：所有API路径配置统一管理
4. **部署灵活**：后端服务地址变更时，只需修改代理配置
5. **代码一致性**：前端代码无需为不同环境修改API路径
6. **微服务架构**：后台管理、C端商城、搜索服务独立部署，互不影响
7. **可观测性**：ELK日志系统 + Prometheus/Grafana监控，全链路可追踪

## 七、常见问题与解决方案

### 7.1 服务启动失败
- **检查日志**：`docker-compose logs 服务名`
- **端口冲突**：确保端口未被占用，Windows可尝试 `net stop winnat && net start winnat` 重置端口
- **依赖问题**：检查数据库和中间件是否正常

### 7.2 代码同步问题
- **检查挂载卷**：确保本地目录正确挂载
- **权限问题**：检查文件权限
- **路径问题**：确保路径配置正确

### 7.3 性能优化
- **资源限制**：在 `docker-compose.yml` 中配置资源限制
- **缓存策略**：合理配置Redis缓存
- **数据库优化**：添加索引，优化查询
- **搜索优化**：合理配置Elasticsearch索引和分片

### 7.4 安全配置
- **环境变量**：敏感信息使用环境变量
- **网络隔离**：使用Docker网络隔离服务
- **访问控制**：配置Nginx访问控制
- **shop-backend仅本机访问**：端口映射为 `127.0.0.1:8081:8081`，外部无法直接访问

---

详细的架构设计请参考 [项目架构与开发部署文档.md](./项目架构与开发部署文档.md)
