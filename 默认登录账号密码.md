# 默认登录账号密码

## 后台管理系统默认账号

从 `docker/mysql/init.sql` 文件中提取的默认登录账号信息：

| 用户名 | 密码 | 角色 | 权限 |
|-------|------|------|------|
| `admin` | `123456` | 超级管理员 | 所有权限 |
| `platform` | `123456` | 平台管理员 | 所有权限 |
| `merchant` | `123456` | 商户账号 | 商品管理、订单管理、活动管理、物流管理、商户查看、商品分类管理、商品SKU管理、支付管理 |
| `pxj` | 非默认密码 | 商户账号 | 商品管理、订单管理、活动管理、物流管理、商户查看、商品分类管理、商品SKU管理、支付管理 |

## C端商城账号

C端商城（customers 表）无预置账号，需通过注册接口创建。

## 登录地址

### 后台管理系统

- **开发环境前端登录页面**：`http://localhost:5173`（Docker 端口映射 5173→3000）
- **生产环境前端登录页面**：`http://admin.服务器IP`
- **后端API地址**：`http://localhost:8080/api`（开发环境）/ `http://服务器IP/api`（生产环境）

### C端商城

- **开发环境前端页面**：`http://localhost:3001`
- **生产环境前端页面**：`http://shop.服务器IP`
- **后端API地址**：`http://localhost:8081/api`（开发环境）/ `http://服务器IP/api`（生产环境）

## 中间件与服务账号

| 服务 | 用户名 | 密码 | 说明 |
|------|--------|------|------|
| MySQL | `root` | `password` | 数据库超级管理员 |
| MySQL | `goshopadmin` | `password` | 应用专用数据库账号 |
| Redis | - | 无密码 | 默认未设置密码 |
| RabbitMQ | `guest` | `guest` | 默认管理员账号，管理界面 http://localhost:15672 |
| Grafana | `admin` | `admin` | 监控面板，http://localhost:3000 |
| Kibana | - | 无需认证 | 日志可视化界面，http://localhost:5601（ES 安全认证已关闭） |
| Elasticsearch | - | 无需认证 | 搜索引擎，http://localhost:9200（xpack.security.enabled=false） |

## 注意事项

1. 首次登录后建议修改密码
2. 不同角色拥有不同的系统权限
3. 超级管理员账号具有最高系统权限
4. `pxj` 账号的密码与默认密码不同，如需使用请联系管理员重置
5. 以上密码均为开发环境默认值，生产环境务必修改
6. Redis 无密码仅限开发环境，生产环境必须设置密码
7. Elasticsearch/Kibana 无认证仅限开发环境，生产环境必须开启安全认证
