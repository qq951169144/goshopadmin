# 数据库表结构说明

## 1. 数据库基本信息

- **数据库名称**: goshopadmin
- **MySQL 版本**: 8.0
- **字符集**: utf8mb4
- **排序规则**: utf8mb4_unicode_ci（所有表统一）
- **引擎**: InnoDB

## 2. 枚举值说明

### 2.1 通用状态 (status)

| 枚举值 | 常量名 | 说明 |
| :--- | :--- | :--- |
| `active` | StatusActive | 启用 |
| `inactive` | StatusInactive | 禁用 |

### 2.2 审核状态 (audit_status)

| 枚举值 | 常量名 | 说明 |
| :--- | :--- | :--- |
| `pending` | AuditStatusPending | 待审核 |
| `approved` | AuditStatusApproved | 已通过 |
| `rejected` | AuditStatusRejected | 已拒绝 |

### 2.3 商户角色 (merchant role)

| 枚举值 | 常量名 | 说明 |
| :--- | :--- | :--- |
| `owner` | MerchantRoleOwner | 老板 |
| `manager` | MerchantRoleManager | 经理 |
| `staff` | MerchantRoleStaff | 员工 |

### 2.4 订单状态 (orders.status)

| 枚举值 | 常量名 | 说明 |
| :--- | :--- | :--- |
| `pending` | OrderStatusPending | 待支付 |
| `paid` | OrderStatusPaid | 已支付/待发货 |
| `shipped` | OrderStatusShipped | 已发货/待收货 |
| `completed` | OrderStatusCompleted | 已完成 |
| `cancelled` | OrderStatusCancelled | 已取消 |

### 2.5 支付状态 (payment_status)

| 枚举值 | 常量名 | 说明 |
| :--- | :--- | :--- |
| `pending` | PaymentStatusPending | 待支付 |
| `success` | PaymentStatusSuccess | 支付成功 |
| `failed` | PaymentStatusFailed | 支付失败 |

### 2.6 物流状态 (shipping_status)

| 枚举值 | 常量名 | 说明 |
| :--- | :--- | :--- |
| `pending` | ShippingStatusPending | 待发货 |
| `shipped` | ShippingStatusShipped | 已发货 |
| `delivered` | ShippingStatusDelivered | 已送达 |
| `returned` | ShippingStatusReturned | 已退回 |

### 2.7 提现状态 (merchant_withdraw.status)

| 枚举值 | 常量名 | 说明 |
| :--- | :--- | :--- |
| `pending` | WithdrawStatusPending | 待处理 |
| `processing` | WithdrawStatusProcessing | 处理中 |
| `completed` | WithdrawStatusCompleted | 已完成 |
| `failed` | WithdrawStatusFailed | 失败 |

### 2.8 对账单状态 (merchant_statement.status)

| 枚举值 | 常量名 | 说明 |
| :--- | :--- | :--- |
| `draft` | MerchantStatementStatusDraft | 草稿 |
| `confirmed` | MerchantStatementStatusConfirmed | 已确认 |
| `settled` | MerchantStatementStatusSettled | 已结算 |

### 2.9 活动类型 (activities.type)

| 枚举值 | 常量名 | 说明 |
| :--- | :--- | :--- |
| `seckill` | ActivityTypeSeckill | 秒杀活动 |
| `redeem_code` | ActivityTypeRedeemCode | 兑换码活动 |

### 2.10 活动商品类型 (activity_products.product_type)

| 枚举值 | 说明 |
| :--- | :--- |
| `seckill` | 秒杀商品 |
| `redeem` | 兑换商品 |

### 2.11 兑换码状态 (redeem_codes.status)

| 枚举值 | 常量名 | 说明 |
| :--- | :--- | :--- |
| `unused` | RedeemCodeStatusUnused | 未使用 |
| `used` | RedeemCodeStatusUsed | 已使用 |
| `expired` | RedeemCodeStatusExpired | 已过期 |
| `disabled` | RedeemCodeStatusDisabled | 已禁用 |

### 2.12 兑换码核销状态 (redeem_code_logs.status)

| 枚举值 | 常量名 | 说明 |
| :--- | :--- | :--- |
| `pending` | RedeemCodeLogStatusPending | 待核销 |
| `verified` | RedeemCodeLogStatusVerified | 已核销 |
| `rejected` | RedeemCodeLogStatusRejected | 已拒绝 |

### 2.13 兑换码类型 (activity_redeem_settings.code_type)

| 枚举值 | 常量名 | 说明 |
| :--- | :--- | :--- |
| `numeric` | RedeemCodeTypeNumeric | 纯数字 |
| `alpha` | RedeemCodeTypeAlpha | 纯字母 |
| `alphanumeric` | RedeemCodeTypeAlphanumeric | 数字+字母 |

### 2.14 SKU活动类型 (product_skus.is_activity)

| 值 | 常量名 | 说明 |
| :--- | :--- | :--- |
| `0` | SkuActivityTypeNormal | 普通SKU |
| `1` | SkuActivityTypeActivity | 活动专用SKU |

## 3. 表结构详情

### 3.1 roles 角色表

**描述**: 存储用户角色信息

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `int` | `NOT NULL AUTO_INCREMENT` | - | 角色ID |
| `name` | `varchar(50)` | `NOT NULL` | - | 角色名称 |
| `description` | `varchar(200)` | `NULL` | - | 角色描述 |
| `status` | `enum('active','inactive')` | `NULL` | `'active'` | 状态（见2.1） |
| `created_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |

**索引**: 主键 `id`

---

### 3.2 permissions 权限表

**描述**: 存储系统权限信息

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `int` | `NOT NULL AUTO_INCREMENT` | - | 权限ID |
| `name` | `varchar(50)` | `NOT NULL` | - | 权限名称 |
| `code` | `varchar(50)` | `NOT NULL` | - | 权限代码 |
| `description` | `varchar(200)` | `NULL` | - | 权限描述 |
| `status` | `enum('active','inactive')` | `NULL` | `'active'` | 状态（见2.1） |
| `category` | `varchar(50)` | `NULL` | - | 权限分类 |
| `created_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |

**索引**: 主键 `id`，普通索引 `idx_permissions_category` (`category`)

---

### 3.3 role_permissions 角色权限关联表

**描述**: 存储角色与权限的多对多对应关系

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `role_id` | `int` | `NOT NULL` | - | 角色ID |
| `permission_id` | `int` | `NOT NULL` | - | 权限ID |

**索引**: 联合主键 (`role_id`, `permission_id`)，普通索引 `idx_role_permissions_role_id` (`role_id`)，`idx_role_permissions_permission_id` (`permission_id`)

---

### 3.4 users 用户表

**描述**: 后台管理系统用户（管理员、运营人员等）

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `int` | `NOT NULL AUTO_INCREMENT` | - | 用户ID |
| `username` | `varchar(191)` | `NOT NULL` | - | 用户名 |
| `password` | `longtext` | `NOT NULL` | - | 密码（bcrypt加密） |
| `role_id` | `int` | `NOT NULL` | - | 角色ID |
| `status` | `enum('active','inactive')` | `NULL` | `'active'` | 状态（见2.1） |
| `created_at` | `datetime(3)` | `NULL` | - | 创建时间 |
| `updated_at` | `datetime(3)` | `NULL` | - | 更新时间 |
| `email` | `varchar(191)` | `NULL` | - | 邮箱 |

**索引**: 主键 `id`，唯一索引 `username`、`email`，普通索引 `idx_users_role_id` (`role_id`)、`idx_users_status` (`status`)

---

### 3.5 merchants 商户表

**描述**: 存储商户基本信息

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `int` | `NOT NULL AUTO_INCREMENT` | - | 商户ID |
| `name` | `varchar(100)` | `NOT NULL` | - | 商户名称 |
| `contact_name` | `varchar(50)` | `NOT NULL` | - | 联系人姓名 |
| `contact_phone` | `varchar(20)` | `NOT NULL` | - | 联系人电话 |
| `email` | `varchar(100)` | `NOT NULL` | - | 电子邮箱 |
| `address` | `varchar(255)` | `NOT NULL` | - | 商户地址 |
| `business_license` | `varchar(255)` | `NOT NULL` | - | 营业执照图片路径 |
| `tax_number` | `varchar(50)` | `NOT NULL` | - | 税务登记号 |
| `audit_status` | `enum('pending','approved','rejected')` | `NULL` | `'pending'` | 审核状态（见2.2） |
| `status` | `enum('active','inactive')` | `NULL` | `'inactive'` | 商户状态（见2.1），默认禁用 |
| `created_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |
| `approved_at` | `datetime` | `NULL` | - | 审核通过时间 |
| `approved_by` | `int` | `NULL` | - | 审核人ID |

**索引**: 主键 `id`，普通索引 `idx_merchants_status` (`status`)、`idx_merchants_audit_status` (`audit_status`)

---

### 3.6 merchant_users 商户用户关联表

**描述**: 存储商户与后台用户的关联关系

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `int` | `NOT NULL AUTO_INCREMENT` | - | 关联ID |
| `merchant_id` | `int` | `NOT NULL` | - | 商户ID |
| `user_id` | `int` | `NOT NULL` | - | 用户ID |
| `role` | `enum('owner','manager','staff')` | `NOT NULL` | `'owner'` | 商户内角色（见2.3） |
| `status` | `enum('active','inactive')` | `NULL` | `'active'` | 状态（见2.1） |
| `created_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |

**索引**: 主键 `id`，唯一索引 `unique_merchant_user` (`merchant_id`, `user_id`)，普通索引 `idx_merchant_users_merchant_id` (`merchant_id`)、`idx_merchant_users_user_id` (`user_id`)

---

### 3.7 merchant_audit 商户审核表

**描述**: 存储商户注册和信息变更的审核记录

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `int` | `NOT NULL AUTO_INCREMENT` | - | 审核记录ID |
| `merchant_id` | `int` | `NOT NULL` | - | 商户ID |
| `audit_type` | `varchar(20)` | `NOT NULL` | - | 审核类型（如registration） |
| `old_data` | `json` | `NULL` | - | 审核前数据 |
| `new_data` | `json` | `NULL` | - | 审核后数据 |
| `status` | `enum('pending','approved','rejected')` | `NULL` | `'pending'` | 审核状态（见2.2） |
| `remark` | `text` | `NULL` | - | 审核备注 |
| `created_by` | `int` | `NOT NULL` | - | 申请人ID |
| `audited_by` | `int` | `NULL` | - | 审核人ID |
| `created_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP` | 创建时间 |
| `audited_at` | `datetime` | `NULL` | - | 审核时间 |

**索引**: 主键 `id`，普通索引 `idx_merchant_audit_merchant_id` (`merchant_id`)、`idx_merchant_audit_status` (`status`)

---

### 3.8 merchant_bank 商户银行信息表

**描述**: 存储商户银行账户信息

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `int` | `NOT NULL AUTO_INCREMENT` | - | 银行信息ID |
| `merchant_id` | `int` | `NOT NULL` | - | 商户ID |
| `bank_name` | `varchar(100)` | `NOT NULL` | - | 银行名称 |
| `account_name` | `varchar(100)` | `NOT NULL` | - | 账户名称 |
| `account_number` | `varchar(50)` | `NOT NULL` | - | 银行账号 |
| `branch` | `varchar(100)` | `NOT NULL` | - | 开户行网点 |
| `is_default` | `tinyint(1)` | `NULL` | `0` | 是否默认账户 |
| `status` | `enum('active','inactive')` | `NULL` | `'active'` | 状态（见2.1） |
| `created_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |

**索引**: 主键 `id`，普通索引 `idx_merchant_bank_merchant_id` (`merchant_id`)、`idx_merchant_bank_is_default` (`is_default`)

---

### 3.9 merchant_withdraw 商户提现记录表

**描述**: 存储商户提现申请和处理记录

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `int` | `NOT NULL AUTO_INCREMENT` | - | 提现记录ID |
| `merchant_id` | `int` | `NOT NULL` | - | 商户ID |
| `amount` | `decimal(10,2)` | `NOT NULL` | - | 提现金额 |
| `bank_id` | `int` | `NOT NULL` | - | 银行信息ID |
| `status` | `enum('pending','processing','completed','failed')` | `NULL` | `'pending'` | 提现状态（见2.7） |
| `order_no` | `varchar(32)` | `NOT NULL` | - | 提现单号 |
| `remark` | `text` | `NULL` | - | 备注 |
| `created_by` | `int` | `NOT NULL` | - | 申请人ID |
| `processed_by` | `int` | `NULL` | - | 处理人ID |
| `created_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP` | 创建时间 |
| `processed_at` | `datetime` | `NULL` | - | 处理时间 |

**索引**: 主键 `id`，唯一索引 `unique_withdraw_order_no` (`order_no`)，普通索引 `idx_merchant_withdraw_merchant_id` (`merchant_id`)、`idx_merchant_withdraw_status` (`status`)

---

### 3.10 merchant_statement 商户对账单表

**描述**: 存储商户对账单信息

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `int` | `NOT NULL AUTO_INCREMENT` | - | 对账单ID |
| `merchant_id` | `int` | `NOT NULL` | - | 商户ID |
| `statement_no` | `varchar(32)` | `NOT NULL` | - | 对账单编号 |
| `start_date` | `date` | `NOT NULL` | - | 开始日期 |
| `end_date` | `date` | `NOT NULL` | - | 结束日期 |
| `total_amount` | `decimal(10,2)` | `NOT NULL` | - | 总金额 |
| `order_count` | `int` | `NOT NULL` | - | 订单数量 |
| `status` | `enum('draft','confirmed','settled')` | `NULL` | `'draft'` | 对账单状态（见2.8） |
| `created_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |

**索引**: 主键 `id`，唯一索引 `unique_statement_no` (`statement_no`)，普通索引 `idx_merchant_statement_merchant_id` (`merchant_id`)、`idx_merchant_statement_status` (`status`)

---

### 3.11 product_categories 商品分类表

**描述**: 存储商品分类信息，支持多级分类

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `int` | `NOT NULL AUTO_INCREMENT` | - | 分类ID |
| `merchant_id` | `int` | `NOT NULL` | - | 商户ID |
| `name` | `varchar(50)` | `NOT NULL` | - | 分类名称 |
| `parent_id` | `int` | `NULL` | `0` | 父分类ID，0表示顶级分类 |
| `level` | `int` | `NULL` | `1` | 分类级别 |
| `sort` | `int` | `NULL` | `0` | 排序权重 |
| `status` | `enum('active','inactive')` | `NULL` | `'active'` | 状态（见2.1） |
| `created_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |

**索引**: 主键 `id`，普通索引 `idx_product_categories_merchant_id` (`merchant_id`)、`idx_product_categories_parent_id` (`parent_id`)、`idx_product_categories_status` (`status`)

---

### 3.12 products 商品表

**描述**: 存储商品基本信息

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `int` | `NOT NULL AUTO_INCREMENT` | - | 商品ID |
| `name` | `varchar(100)` | `NOT NULL` | - | 商品名称 |
| `description` | `longtext` | `NULL` | - | 商品描述 |
| `detail` | `text` | `NULL` | - | 商品详情（富文本） |
| `price` | `decimal(10,2)` | `NOT NULL` | - | 商品价格（展示用，实际价格以SKU为准） |
| `stock` | `int` | `NOT NULL` | `0` | 库存数量（所有SKU库存之和） |
| `category_id` | `int` | `NOT NULL` | - | 分类ID |
| `merchant_id` | `int` | `NOT NULL` | - | 商户ID |
| `status` | `enum('active','inactive')` | `NULL` | `'active'` | 状态（见2.1） |
| `is_activity` | `tinyint` | `NOT NULL` | `0` | 是否活动商品：0-否，1-是 |
| `created_at` | `datetime(3)` | `NULL` | - | 创建时间 |
| `updated_at` | `datetime(3)` | `NULL` | - | 更新时间 |

**索引**: 主键 `id`，普通索引 `idx_products_merchant_id` (`merchant_id`)、`idx_products_category_id` (`category_id`)、`idx_products_status` (`status`)、`idx_products_is_activity` (`is_activity`)

---

### 3.13 product_skus 商品SKU表

**描述**: 存储商品SKU信息，每个SKU有独立的编码、价格和库存

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `int` | `NOT NULL AUTO_INCREMENT` | - | SKU ID |
| `product_id` | `int` | `NOT NULL` | - | 商品ID |
| `merchant_id` | `int` | `NOT NULL` | - | 商户ID |
| `sku_code` | `varchar(50)` | `NOT NULL` | - | SKU编码 |
| `attributes` | `json` | `NULL` | - | SKU属性（JSON格式） |
| `price` | `decimal(10,2)` | `NOT NULL` | - | SKU价格 |
| `original_price` | `decimal(10,2)` | `NULL` | `0.00` | 原价 |
| `stock` | `int` | `NOT NULL` | - | SKU库存 |
| `is_activity` | `tinyint(1)` | `NULL` | `0` | 是否活动专用SKU（见2.14） |
| `activity_id` | `int` | `NULL` | - | 关联活动ID（当is_activity=1时有效） |
| `status` | `enum('active','inactive')` | `NULL` | `'active'` | 状态（见2.1） |
| `created_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |

**索引**: 主键 `id`，唯一索引 `unique_sku_code` (`sku_code`)，普通索引 `idx_product_skus_product_id` (`product_id`)、`idx_product_skus_merchant_id` (`merchant_id`)、`idx_product_skus_status` (`status`)、`idx_product_skus_is_activity` (`is_activity`)、`idx_product_skus_activity_id` (`activity_id`)

---

### 3.14 product_specifications 商品规格表

**描述**: 存储商品的规格定义（如颜色、尺寸等）

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `int` | `NOT NULL AUTO_INCREMENT` | - | 规格ID |
| `product_id` | `int` | `NOT NULL` | - | 所属商品ID |
| `name` | `varchar(50)` | `NOT NULL` | - | 规格名称（如：颜色、尺寸、版本） |
| `sort` | `int` | `NULL` | `0` | 排序，控制前端展示顺序 |
| `created_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |

**索引**: 主键 `id`，普通索引 `idx_product_specifications_product_id` (`product_id`)

---

### 3.15 product_specification_values 规格值表

**描述**: 存储规格的具体值（如红色、M码等）

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `int` | `NOT NULL AUTO_INCREMENT` | - | 规格值ID |
| `spec_id` | `int` | `NOT NULL` | - | 关联规格ID |
| `value` | `varchar(50)` | `NOT NULL` | - | 规格值（如：红色、M码） |
| `sort` | `int` | `NULL` | `0` | 排序 |
| `status` | `enum('active','inactive')` | `NULL` | `'active'` | 状态（见2.1） |
| `created_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |

**索引**: 主键 `id`，普通索引 `idx_specification_values_spec_id` (`spec_id`)

---

### 3.16 product_sku_specs SKU规格关联表

**描述**: 存储SKU与规格值的关联关系，支持多规格组合

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `int` | `NOT NULL AUTO_INCREMENT` | - | 关联ID |
| `sku_id` | `int` | `NOT NULL` | - | SKU ID |
| `spec_id` | `int` | `NOT NULL` | - | 规格ID |
| `spec_value_id` | `int` | `NOT NULL` | - | 规格值ID |
| `created_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP` | 创建时间 |

**索引**: 主键 `id`，普通索引 `idx_sku_specs_sku_id` (`sku_id`)、`idx_sku_specs_spec_id` (`spec_id`)、`idx_sku_specs_spec_value_id` (`spec_value_id`)

---

### 3.17 product_images 商品图片表

**描述**: 存储商品图片信息

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `int` | `NOT NULL AUTO_INCREMENT` | - | 图片ID |
| `product_id` | `int` | `NOT NULL` | - | 商品ID |
| `image_url` | `varchar(255)` | `NOT NULL` | - | 图片路径 |
| `is_main` | `tinyint(1)` | `NULL` | `0` | 是否主图 |
| `sort` | `int` | `NULL` | `0` | 排序权重 |
| `created_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |

**索引**: 主键 `id`，普通索引 `idx_product_images_product_id` (`product_id`)、`idx_product_images_is_main` (`is_main`)

---

### 3.18 customers C端用户表

**描述**: 存储C端商城消费者/客户信息

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `int` | `NOT NULL AUTO_INCREMENT` | - | 客户ID |
| `username` | `varchar(50)` | `NOT NULL` | - | 用户名 |
| `password` | `varchar(100)` | `NOT NULL` | - | 密码 |
| `phone` | `varchar(20)` | `NULL` | - | 手机号 |
| `email` | `varchar(100)` | `NULL` | - | 电子邮箱 |
| `status` | `enum('active','inactive')` | `NULL` | `'active'` | 状态（见2.1） |
| `created_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |
| `nickname` | `varchar(50)` | `NULL` | - | 昵称 |
| `avatar` | `text` | `NULL` | - | 头像 |
| `last_login_at` | `datetime` | `NULL` | - | 最后登录时间 |
| `last_login_ip` | `varchar(50)` | `NULL` | - | 最后登录IP |

**索引**: 主键 `id`，唯一索引 `username`、`phone`、`email`

---

### 3.19 addresses 地址表

**描述**: 存储C端用户收货地址信息

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `int` | `NOT NULL AUTO_INCREMENT` | - | 地址ID |
| `customer_id` | `int` | `NOT NULL` | - | 客户ID |
| `name` | `varchar(50)` | `NOT NULL` | - | 收货人姓名 |
| `phone` | `varchar(20)` | `NOT NULL` | - | 联系电话 |
| `province` | `varchar(50)` | `NOT NULL` | - | 省份 |
| `city` | `varchar(50)` | `NOT NULL` | - | 城市 |
| `district` | `varchar(50)` | `NOT NULL` | - | 区县 |
| `detail_address` | `varchar(255)` | `NOT NULL` | - | 详细地址 |
| `is_default` | `tinyint(1)` | `NULL` | `0` | 是否默认地址 |
| `status` | `enum('active','inactive')` | `NULL` | `'active'` | 状态（见2.1） |
| `created_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |

**索引**: 主键 `id`，普通索引 `idx_addresses_customer_id` (`customer_id`)、`idx_addresses_is_default` (`is_default`)

---

### 3.20 orders 订单表

**描述**: 存储订单信息

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `int` | `NOT NULL AUTO_INCREMENT` | - | 订单ID |
| `order_no` | `varchar(32)` | `NOT NULL` | - | 订单号 |
| `customer_id` | `int` | `NOT NULL` | - | 客户ID |
| `merchant_id` | `int` | `NOT NULL` | - | 商户ID |
| `activity_id` | `int` | `NULL` | `0` | 活动ID，0表示非活动订单 |
| `total_amount` | `decimal(10,2)` | `NOT NULL` | - | 总金额 |
| `status` | `enum('pending','paid','shipped','completed','cancelled')` | `NOT NULL` | `'pending'` | 订单状态（见2.4） |
| `payment_status` | `enum('pending','success','failed')` | `NOT NULL` | `'pending'` | 支付状态（见2.5） |
| `shipping_status` | `enum('pending','shipped','delivered','returned')` | `NOT NULL` | `'pending'` | 物流状态（见2.6） |
| `address_id` | `int` | `NOT NULL` | - | 收货地址ID |
| `created_at` | `datetime(3)` | `NULL` | - | 创建时间 |
| `updated_at` | `datetime(3)` | `NULL` | - | 更新时间 |
| `payment_method` | `longtext` | `NULL` | - | 支付方式 |
| `transaction_id` | `longtext` | `NULL` | - | 交易ID |
| `paid_at` | `datetime` | `NULL` | - | 支付时间 |
| `shipped_at` | `datetime` | `NULL` | - | 发货时间 |
| `delivered_at` | `datetime` | `NULL` | - | 送达时间 |
| `cancelled_at` | `datetime` | `NULL` | - | 取消时间 |

**索引**: 主键 `id`，唯一索引 `order_no`，普通索引 `idx_orders_customer_id` (`customer_id`)、`idx_orders_merchant_id` (`merchant_id`)、`idx_orders_activity_id` (`activity_id`)、`idx_orders_address_id` (`address_id`)、`idx_orders_status` (`status`)、`idx_orders_payment_status` (`payment_status`)、`idx_orders_shipping_status` (`shipping_status`)

---

### 3.21 order_items 订单明细表

**描述**: 存储订单中的商品明细

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `int` | `NOT NULL AUTO_INCREMENT` | - | 明细ID |
| `order_id` | `int` | `NOT NULL` | - | 订单ID |
| `product_id` | `int` | `NOT NULL` | - | 商品ID |
| `sku_id` | `int` | `NULL` | - | SKU ID |
| `product_name` | `varchar(100)` | `NOT NULL` | - | 商品名称（下单时快照） |
| `sku_attributes` | `json` | `NULL` | - | SKU属性（下单时快照） |
| `price` | `decimal(10,2)` | `NOT NULL` | - | 商品单价（下单时快照） |
| `quantity` | `int` | `NOT NULL` | - | 购买数量 |
| `total_amount` | `decimal(10,2)` | `NOT NULL` | - | 明细总金额 |
| `created_at` | `datetime(3)` | `NULL` | - | 创建时间 |
| `updated_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |

**索引**: 主键 `id`，普通索引 `idx_order_items_order_id` (`order_id`)、`idx_order_items_product_id` (`product_id`)

---

### 3.22 payments 支付记录表

**描述**: 存储订单支付信息

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `int` | `NOT NULL AUTO_INCREMENT` | - | 支付记录ID |
| `order_id` | `int` | `NOT NULL` | - | 订单ID |
| `payment_no` | `varchar(32)` | `NOT NULL` | - | 支付单号 |
| `amount` | `decimal(10,2)` | `NOT NULL` | - | 支付金额 |
| `payment_method` | `varchar(20)` | `NOT NULL` | - | 支付方式代码 |
| `transaction_id` | `varchar(100)` | `NULL` | - | 第三方交易ID |
| `status` | `enum('pending','success','failed')` | `NULL` | `'pending'` | 支付状态（见2.5） |
| `created_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |
| `paid_at` | `datetime` | `NULL` | - | 支付时间 |

**索引**: 主键 `id`，唯一索引 `unique_payment_no` (`payment_no`)，普通索引 `idx_payments_order_id` (`order_id`)、`idx_payments_status` (`status`)

---

### 3.23 activities 活动表

**描述**: 存储系统活动信息

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `int` | `NOT NULL AUTO_INCREMENT` | - | 活动ID |
| `merchant_id` | `int` | `NOT NULL` | - | 商户ID |
| `name` | `varchar(100)` | `NOT NULL` | - | 活动名称 |
| `type` | `enum('seckill','redeem_code')` | `NOT NULL` | - | 活动类型（见2.9） |
| `start_time` | `datetime` | `NOT NULL` | - | 开始时间 |
| `end_time` | `datetime` | `NOT NULL` | - | 结束时间 |
| `status` | `enum('active','inactive')` | `NULL` | `'active'` | 状态（见2.1） |
| `created_by` | `int` | `NOT NULL` | - | 创建人ID |
| `created_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |

**索引**: 主键 `id`，普通索引 `idx_activities_merchant_id` (`merchant_id`)、`idx_activities_created_by` (`created_by`)

---

### 3.24 activity_products 活动商品表

**描述**: 存储参与活动的商品信息

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `int` | `NOT NULL AUTO_INCREMENT` | - | 活动商品ID |
| `activity_id` | `int` | `NOT NULL` | - | 活动ID |
| `product_id` | `int` | `NOT NULL` | - | 商品ID |
| `sku_id` | `int` | `NULL` | - | SKU ID |
| `merchant_id` | `int` | `NOT NULL` | - | 商户ID |
| `product_type` | `enum('seckill','redeem')` | `NULL` | `'seckill'` | 商品在活动中的类型（见2.10） |
| `status` | `enum('active','inactive')` | `NULL` | `'active'` | 状态（见2.1） |
| `created_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |

**索引**: 主键 `id`，普通索引 `idx_activity_products_activity_id` (`activity_id`)、`idx_activity_products_product_id` (`product_id`)、`idx_activity_products_merchant_id` (`merchant_id`)、`idx_activity_products_product_type` (`product_type`)

---

### 3.25 activity_stats 活动效果统计表

**描述**: 存储活动效果统计数据

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `int` | `NOT NULL AUTO_INCREMENT` | - | 统计ID |
| `activity_id` | `int` | `NOT NULL` | - | 活动ID |
| `view_count` | `int` | `NULL` | `0` | 浏览次数 |
| `participant_count` | `int` | `NULL` | `0` | 参与人数 |
| `order_count` | `int` | `NULL` | `0` | 订单数量 |
| `total_amount` | `decimal(10,2)` | `NULL` | `0.00` | 总金额 |
| `created_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |

**索引**: 主键 `id`，普通索引 `idx_activity_stats_activity_id` (`activity_id`)

---

### 3.26 redeem_codes 兑换码表

**描述**: 存储兑换码信息

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `int` | `NOT NULL AUTO_INCREMENT` | - | 兑换码ID |
| `activity_id` | `int` | `NOT NULL` | - | 关联活动ID |
| `merchant_id` | `int` | `NOT NULL` | - | 商户ID |
| `code` | `varchar(50)` | `NOT NULL` | - | 兑换码 |
| `phone` | `varchar(20)` | `NULL` | - | 用户手机号（导入时使用） |
| `status` | `enum('unused','used','expired','disabled')` | `NULL` | `'unused'` | 兑换码状态（见2.11） |
| `used_by` | `int` | `NULL` | - | 使用人ID（customer_id） |
| `used_at` | `datetime` | `NULL` | - | 使用时间 |
| `valid_start_time` | `datetime` | `NULL` | - | 有效期开始时间 |
| `valid_end_time` | `datetime` | `NULL` | - | 有效期结束时间 |
| `created_by` | `int` | `NOT NULL` | - | 创建人ID |
| `created_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |

**索引**: 主键 `id`，唯一索引 `unique_redeem_code` (`code`)，普通索引 `idx_redeem_codes_activity_id` (`activity_id`)、`idx_redeem_codes_merchant_id` (`merchant_id`)、`idx_redeem_codes_status` (`status`)、`idx_redeem_codes_used_by` (`used_by`)、`idx_redeem_codes_phone` (`phone`)

---

### 3.27 activity_redeem_settings 兑换码活动配置表

**描述**: 存储兑换码活动的配置信息

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `int` | `NOT NULL AUTO_INCREMENT` | - | 配置ID |
| `activity_id` | `int` | `NOT NULL` | - | 关联活动ID |
| `merchant_id` | `int` | `NOT NULL` | - | 商户ID |
| `code_type` | `varchar(20)` | `NOT NULL` | `'alphanumeric'` | 兑换码类型（见2.13） |
| `code_length` | `int` | `NOT NULL` | `8` | 兑换码长度 |
| `exclude_chars` | `varchar(20)` | `NULL` | `'IO10'` | 排除的字符（避免混淆） |
| `total_quantity` | `int` | `NOT NULL` | `0` | 兑换码总数量 |
| `limit_per_user` | `int` | `NULL` | `1` | 每人限兑次数（0表示不限） |
| `need_verify` | `tinyint(1)` | `NULL` | `1` | 是否需要后台核销：0-否，1-是 |
| `created_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |

**索引**: 主键 `id`，唯一索引 `unique_activity_redeem_setting` (`activity_id`)，普通索引 `idx_activity_redeem_settings_merchant_id` (`merchant_id`)

---

### 3.28 redeem_code_logs 兑换码核销记录表

**描述**: 存储兑换码的核销记录

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `int` | `NOT NULL AUTO_INCREMENT` | - | 核销记录ID |
| `redeem_code_id` | `int` | `NOT NULL` | - | 兑换码ID |
| `activity_id` | `int` | `NOT NULL` | - | 活动ID |
| `merchant_id` | `int` | `NOT NULL` | - | 商户ID |
| `customer_id` | `int` | `NOT NULL` | - | 客户ID |
| `code` | `varchar(50)` | `NOT NULL` | - | 兑换码 |
| `verify_by` | `int` | `NULL` | - | 核销人ID（后台管理员） |
| `verify_at` | `datetime` | `NULL` | - | 核销时间 |
| `status` | `enum('pending','verified','rejected')` | `NULL` | `'pending'` | 核销状态（见2.12） |
| `remark` | `varchar(255)` | `NULL` | - | 备注 |
| `created_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |

**索引**: 主键 `id`，普通索引 `idx_redeem_code_logs_redeem_code_id` (`redeem_code_id`)、`idx_redeem_code_logs_activity_id` (`activity_id`)、`idx_redeem_code_logs_merchant_id` (`merchant_id`)、`idx_redeem_code_logs_customer_id` (`customer_id`)、`idx_redeem_code_logs_status` (`status`)、`idx_redeem_code_logs_verify_by` (`verify_by`)

---

### 3.29 shipping 物流表

**描述**: 存储订单物流信息

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `int` | `NOT NULL AUTO_INCREMENT` | - | 物流ID |
| `order_id` | `int` | `NOT NULL` | - | 订单ID |
| `tracking_no` | `varchar(50)` | `NULL` | - | 物流单号 |
| `status` | `enum('pending','shipped','delivered','returned')` | `NOT NULL` | `'pending'` | 物流状态（见2.6） |
| `created_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |

**索引**: 主键 `id`，普通索引 `idx_shipping_order_id` (`order_id`)

---

### 3.30 carts 购物车表

**描述**: 存储C端用户购物车信息

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `int` | `NOT NULL AUTO_INCREMENT` | - | 购物车ID |
| `customer_id` | `int` | `NOT NULL` | `0` | 客户ID（登录状态） |
| `session_id` | `varchar(255)` | `NULL` | - | 会话ID（未登录状态） |
| `created_at` | `datetime(3)` | `NULL` | - | 创建时间 |
| `updated_at` | `datetime(3)` | `NULL` | `CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3)` | 更新时间 |

**索引**: 主键 `id`，普通索引 `idx_carts_customer_id` (`customer_id`)、`idx_carts_session_id` (`session_id`)、`idx_carts_customer_session` (`customer_id`, `session_id`)

---

### 3.31 cart_items 购物车项表

**描述**: 存储购物车中的商品信息

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `int` | `NOT NULL AUTO_INCREMENT` | - | 购物车项ID |
| `cart_id` | `int` | `NOT NULL` | - | 购物车ID |
| `product_id` | `int` | `NOT NULL` | - | 商品ID |
| `sku_id` | `int` | `NULL` | - | SKU ID |
| `quantity` | `int` | `NOT NULL` | `1` | 商品数量 |
| `price` | `decimal(10,2)` | `NOT NULL` | - | 商品价格 |
| `created_at` | `datetime(3)` | `NULL` | - | 创建时间 |
| `updated_at` | `datetime(3)` | `NULL` | - | 更新时间 |

**索引**: 主键 `id`，普通索引 `idx_cart_items_cart_id` (`cart_id`)、`idx_cart_items_product_id` (`product_id`)、`idx_cart_items_sku_id` (`sku_id`)、`idx_cart_items_cart_product` (`cart_id`, `product_id`)

---

### 3.32 payment_methods 支付方式表

**描述**: 存储系统支持的支付方式

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `int` | `NOT NULL AUTO_INCREMENT` | - | 支付方式ID |
| `name` | `varchar(50)` | `NOT NULL` | - | 支付方式名称 |
| `code` | `varchar(20)` | `NOT NULL` | - | 支付方式代码 |
| `status` | `enum('active','inactive')` | `NULL` | `'active'` | 状态（见2.1） |
| `created_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |

**索引**: 主键 `id`，唯一索引 `unique_code` (`code`)

---

### 3.33 shipping_methods 物流方式表

**描述**: 存储系统支持的物流方式

| 字段名 | 数据类型 | 约束 | 默认值 | 描述 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `int` | `NOT NULL AUTO_INCREMENT` | - | 物流方式ID |
| `name` | `varchar(50)` | `NOT NULL` | - | 物流方式名称 |
| `code` | `varchar(20)` | `NOT NULL` | - | 物流方式代码 |
| `price` | `decimal(10,2)` | `NOT NULL` | - | 物流价格 |
| `status` | `enum('active','inactive')` | `NULL` | `'active'` | 状态（见2.1） |
| `created_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | `datetime` | `NULL` | `CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` | 更新时间 |

**索引**: 主键 `id`，唯一索引 `unique_code` (`code`)

---

## 4. 表关系图

### 4.1 用户与权限系统

```
users ──(role_id)──► roles ◄──(role_id)──► role_permissions ◄──(permission_id)──► permissions
  │
  └──(user_id)──► merchant_users ◄──(merchant_id)──► merchants
```

### 4.2 商户管理系统

```
merchants ──┬──► merchant_audit（审核记录）
            ├──► merchant_bank（银行信息）
            ├──► merchant_withdraw（提现记录）
            ├──► merchant_statement（对账单）
            ├──► product_categories（商品分类）
            ├──► products（商品）
            └──► activities（活动）
```

### 4.3 商品规格系统

```
products ──┬──► product_skus（SKU）
           ├──► product_images（图片）
           ├──► product_specifications（规格定义）
           │         │
           │         └──► product_specification_values（规格值）
           │
           └──► product_sku_specs（SKU与规格值关联）
                    │
                    ├──► product_skus
                    ├──► product_specifications
                    └──► product_specification_values
```

### 4.4 订单系统

```
customers ──┬──► addresses（收货地址）
            └──► orders ──┬──► order_items（订单明细）
                          │         │
                          │         ├──► products
                          │         └──► product_skus
                          ├──► payments（支付记录）
                          └──► shipping（物流信息）
```

### 4.5 活动系统

```
activities ──┬──► activity_products（活动商品）
             │         │
             │         ├──► products
             │         └──► product_skus
             ├──► activity_stats（活动统计）
             ├──► redeem_codes（兑换码）
             └──► activity_redeem_settings（兑换码配置）
                       │
                       └──► redeem_code_logs（核销记录）
```

### 4.6 购物车系统

```
customers ──► carts ──► cart_items
                          │
                          ├──► products
                          └──► product_skus
```

## 5. 初始数据

### 5.1 角色数据

| ID | 名称 | 描述 |
| :--- | :--- | :--- |
| 1 | 超级管理员 | 系统最高权限 |
| 2 | 平台管理员 | 平台管理权限 |
| 3 | 商户账号 | 商户管理权限 |

### 5.2 权限数据

| ID | 名称 | 代码 | 描述 | 分类 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | 用户管理 | user:manage | 管理用户账号 | user |
| 2 | 角色管理 | role:manage | 管理角色权限 | role |
| 3 | 商品管理 | product:manage | 管理商品信息 | product |
| 4 | 订单管理 | order:manage | 管理订单信息 | order |
| 5 | 活动管理 | activity:manage | 管理活动信息 | activity |
| 6 | 物流管理 | shipping:manage | 管理物流信息 | shipping |
| 7 | 商户管理 | merchant:manage | 管理商户信息 | merchant |
| 8 | 商户审核 | merchant:audit | 审核商户注册和信息更新 | merchant |
| 9 | 商户财务 | merchant:finance | 管理商户提现和对账 | merchant |
| 10 | 商户查看 | merchant:view | 查看商户信息 | merchant |
| 11 | 商品分类管理 | product:category | 管理商品分类 | product |
| 12 | 商品SKU管理 | product:sku | 管理商品SKU | product |
| 13 | 支付管理 | order:payment | 管理订单支付 | order |
| 14 | 活动统计 | activity:stats | 查看活动统计数据 | activity |

### 5.3 默认用户数据

| ID | 用户名 | 角色ID | 状态 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | admin | 1 | active | 超级管理员（密码：123456） |
| 2 | platform | 2 | active | 平台管理员（密码：123456） |
| 3 | merchant | 3 | active | 商户账号（密码：123456） |
| 4 | pxj | 3 | active | 测试商户账号 |

### 5.4 测试商户数据

| ID | 名称 | 联系人 | 联系电话 | 邮箱 | 地址 | 审核状态 | 状态 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | 小商店 | 法人 | 18819279587 | 951169144@qq.com | 广州 | approved | active |

### 5.5 测试商品分类数据

| ID | 商户ID | 名称 | 父分类ID | 级别 | 排序 | 状态 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | 1 | 日用品 | 0 | 1 | 1 | active |
| 2 | 1 | 小玩具 | 0 | 1 | 2 | active |
| 3 | 1 | 食物 | 0 | 1 | 0 | active |
| 4 | 1 | 虚拟商品 | 0 | 1 | 0 | active |

## 6. 设计说明

1. **用户权限系统**: 采用 RBAC 模型，通过 roles、permissions 和 role_permissions 表实现权限管理。后台用户(users)和C端用户(customers)分离存储。

2. **商户管理系统**: 包含商户信息、审核、银行账户、提现和对账功能。商户与后台用户通过 merchant_users 表多对多关联。

3. **商品规格系统**: 支持多规格组合，通过 product_specifications（规格定义）→ product_specification_values（规格值）→ product_sku_specs（SKU与规格值关联）实现。

4. **订单系统**: 订单包含三个维度的状态——订单状态、支付状态、物流状态，分别独立跟踪。订单明细记录下单时的商品快照。

5. **活动系统**: 支持秒杀和兑换码两种活动类型。兑换码活动有独立的配置表(activity_redeem_settings)和核销记录表(redeem_code_logs)。

6. **购物车系统**: 支持登录态(customer_id)和未登录态(session_id)两种购物车，通过联合索引优化查询。

7. **数据完整性**: 不使用数据库外键约束，由应用代码控制数据一致性，提高灵活性和性能。

8. **字符集**: 所有表统一使用 utf8mb4 字符集和 utf8mb4_unicode_ci 排序规则，支持中文和表情符号。
